#include <iostream>
#define _ ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0);

using namespace std;


int main()
{_
    int n, a, b, max = 0, sum[100];
    cin >> n;
    for (int i = 0; i < n; i++)
    {
        cin >> a >> b;
        if (max < b) max = b;
    }

    for (int i = 0; i < b; i++)
    {
        
    }


}