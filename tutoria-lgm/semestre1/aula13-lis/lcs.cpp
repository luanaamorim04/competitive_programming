#include <iostream>

using namespace std;

int freq[MAXN], a[MAXN], b[MAXN];

int32_t main()
{
	cin >> n;
	for (int i = 1; i <= n; i++) cin >> a[i];
	for (int i = 1; i <= n; i++)
	{
		cin >> b[i];
		freq[b[i]]++;
		freq[a[i]]++;
		if (freq[])
	}
}
